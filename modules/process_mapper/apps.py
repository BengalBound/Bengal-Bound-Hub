from django.apps import AppConfig

class ProcessMapperConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'modules.process_mapper'
    label = 'process_mapper'
    verbose_name = 'Process Mapper'
