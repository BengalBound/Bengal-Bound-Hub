from django.apps import AppConfig


class PayloadConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "agents.payload"
    verbose_name = "Payload — Procurement Agent"
