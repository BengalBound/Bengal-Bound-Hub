from django.apps import AppConfig


class ClarityConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "agents.clarity"
    verbose_name = "Clarity — Feedback & Insights"
