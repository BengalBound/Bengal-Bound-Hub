# ============================================================
# Bengal Bound — Voice Receptionist Module
# requirements_voice.txt — ADD to your existing requirements.txt
# ============================================================

# Firebase Authentication
firebase-admin>=6.0.0

# Cache / Session (replaces Redis)
diskcache>=5.6.0

# Task Scheduling (replaces Celery + Redis)
django-apscheduler>=0.6.2
APScheduler>=3.10.0

# Google Cloud APIs
google-cloud-texttospeech>=2.16.0
google-cloud-speech>=2.24.0
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.120.0

# Gemini AI (NLU engine)
google-generativeai>=0.7.0

# Telephony
twilio>=9.0.0

# Token encryption (calendar OAuth tokens)
cryptography>=42.0.0
