# Empty — DJANGO_SETTINGS_MODULE selects the active config file.
# Default: bengalbound_core.settings.development (set in manage.py)
# Production: set DJANGO_SETTINGS_MODULE=bengalbound_core.settings.production
#             in the server environment (not in .env — that file isn't used in prod).
